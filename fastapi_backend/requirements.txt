# FastAPI Backend Dependencies
# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Machine Learning
# NOTE: Model was saved with very old TensorFlow version (uses batch_shape)
# Using TensorFlow 2.10 which still supports old model formats
# If you get DLL errors on Windows, use: pip install tensorflow-cpu==2.10.0
tensorflow==2.10.0
numpy==1.23.5

# Computer Vision
# Using older opencv version compatible with numpy 1.x
opencv-python==4.8.1.78

# Utilities
# IMPORTANT: FastAPI 0.104.1 requires Pydantic v2.x
# TypeAdapter is available in Pydantic v2.0+
pydantic>=2.0.0,<3.0.0
python-dotenv==1.0.0

# MongoDB
pymongo>=4.6.0
dnspython>=2.4.2
certifi>=2023.0.0  # Required for MongoDB Atlas SSL connections

# Email (built-in smtplib is used, no additional package needed)
# Note: For production, consider using a service like SendGrid, Mailgun, or AWS SES
