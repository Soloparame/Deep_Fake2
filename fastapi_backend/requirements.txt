# FastAPI Backend Dependencies
# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Machine Learning (local .keras model only; Render uses requirements-render.txt + HF API)
# TensorFlow needs Python 3.9–3.11. On Render set PYTHON_VERSION=3.10.13 or use requirements-render.txt.
# Windows DLL issues: pip install tensorflow-cpu==2.10.0
tensorflow==2.15.0; python_version < "3.12"
numpy==1.23.5

# Computer Vision
# Using older opencv version compatible with numpy 1.x
opencv-python==4.8.1.78

# HTTP client (for Hugging Face inference API)
requests>=2.28.0
huggingface_hub>=0.26.0
langchain-community>=0.2.0
duckduckgo-search>=6.0.0
groq>=0.11.0

# Utilities
# IMPORTANT: FastAPI 0.104.1 requires Pydantic v2.x
# TypeAdapter is available in Pydantic v2.0+
pydantic>=2.0.0,<3.0.0
python-dotenv==1.0.0

# Document text extraction (plagiarism / analysis)
pymupdf>=1.23.0
python-docx>=1.1.0
python-pptx>=0.6.21

# MongoDB
pymongo>=4.6.0
dnspython>=2.4.2
certifi>=2023.0.0  # Required for MongoDB Atlas SSL connections

# Email (built-in smtplib is used, no additional package needed)
# Note: For production, consider using a service like SendGrid, Mailgun, or AWS SES
